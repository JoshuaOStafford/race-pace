import React from 'react';

const RaceResults = ({hours, minutes, seconds, distance}) => {
    return(
        <>
        <h4>Calculating Results</h4>
        </>
    );
}

export default RaceResults;